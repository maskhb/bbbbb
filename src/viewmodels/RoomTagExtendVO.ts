/**
 * RoomTagExtendVO
 * 房间和标签关联对象
 */
export default class RoomTagExtendVO {
  /**
   * 房间id
   */
  roomId?: number; // int64
  /**
   * 标签id
   */
  tagId?: number; // int64
  /**
   * 标签名称
   */
  tagName?: string; 
}
